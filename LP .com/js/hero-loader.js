/**
 * Laptop Duhok — شرائح Hero من Supabase (hero_slides) + بديل ثابت
 */
(function () {
  var FALLBACK = [
    {
      kicker_ar: "ستاف دهوك — حلول رقمية",
      kicker_ckb: "ستاف دهۆک — چارەسەری دیجیتاڵی",
      kicker_bad: "ستاف دهۆک — چارەسەریێن دیجیتالی",
      title_ar: "نبني مواقعكم وبرمجياتكم باحتراف",
      title_ckb: "ماڵپەڕ و نەرمەکاڵاکانتان بە پیشەیی دروست دەکەین",
      title_bad: "مالپەر و بەرنامەیێن هەوە ب شێوەیەکێ پێشکەفتی چێدکەین",
      description_ar: "من دهوك إلى العالم — تصاميم عصرية، شفافية في الأسعار، ودعم يعزز ثقة عملائكم.",
      description_ckb: "لە دهۆکەوە بۆ جیهان — دیزاینی نوێ، نرخی ڕوون، پشتگیری بۆ متمانەی کڕیاران.",
      description_bad: "ژ دهۆکێ بۆ جیهانێ — دیزاینێن نوی، بهایێن دیار، پشتەڤانی بۆ باوەريا کریاران.",
      primary_btn_text_ar: "اطلب استشارة",
      primary_btn_text_ckb: "داوای ڕاوێژ بکە",
      primary_btn_text_bad: "داخوازا شیرەتکاریێ بکە",
      primary_btn_url: "#contact",
      secondary_btn_text_ar: "تصفح الأعمال",
      secondary_btn_text_ckb: "پۆرتفۆلیۆ ببینە",
      secondary_btn_text_bad: "کارێن مە ببینە",
      secondary_btn_url: "#portfolio",
      featured_image_url: "",
      featured_label_ar: "Laptop Duhok",
      featured_label_ckb: "Laptop Duhok",
      featured_label_bad: "Laptop Duhok",
      background_css: "",
    },
    {
      kicker_ar: "ستاف دهوك — حلول رقمية",
      kicker_ckb: "ستاف دهۆک — چارەسەری دیجیتاڵی",
      kicker_bad: "ستاف دهۆک — چارەسەریێن دیجیتالی",
      title_ar: "تحول رقمي يعكس جودة فريقكم",
      title_ckb: "گۆڕانی دیجیتاڵی کە کوالیتی تیمەکەتان دەردەخات",
      title_bad: "گۆهرینا دیجیتالی کو کوالیتیا تیما هەوە دیار دکەت",
      description_ar: "معرض أعمال يبرز مهاراتكم، وهوية بصرية تواكب أهدافكم التسويقية.",
      description_ckb: "پۆرتفۆلیۆ کە لێهاتوویی دەردەخات و ناسنامەی بینراو هاوشێوەی ئامانجەکانی بازاڕکردن.",
      description_bad: "کارێن مە کو شیانان دیار دکەن و ناسنامەیا دیتنێ ل گەل ئارمانجێن بازارکرنێ.",
      primary_btn_text_ar: "اطلب استشارة",
      primary_btn_text_ckb: "داوای ڕاوێژ بکە",
      primary_btn_text_bad: "داخوازا شیرەتکاریێ بکە",
      primary_btn_url: "#contact",
      secondary_btn_text_ar: "تصفح الأعمال",
      secondary_btn_text_ckb: "پۆرتفۆلیۆ ببینە",
      secondary_btn_text_bad: "کارێن مە ببینە",
      secondary_btn_url: "#portfolio",
      featured_image_url: "",
      featured_label_ar: "Laptop Duhok",
      featured_label_ckb: "Laptop Duhok",
      featured_label_bad: "Laptop Duhok",
      background_css: "",
    },
    {
      kicker_ar: "ستاف دهوك — حلول رقمية",
      kicker_ckb: "ستاف دهۆک — چارەسەری دیجیتاڵی",
      kicker_bad: "ستاف دهۆک — چارەسەریێن دیجیتالی",
      title_ar: "مرجع تقني للباحثين عن الأفضل في دهوك",
      title_ckb: "سەرچاوەی تەکنیکی بۆ ئەوانەی لە دهۆک باشترین دەگەڕێن",
      title_bad: "ژێدەرێ تەکنیکی بۆ وان یێن ل دهۆکێ ل باشترینان دگەرهن",
      description_ar: "محتوى واضح لمحركات البحث وللأسئلة الذكية — لنكون الخطوة الأولى عند سؤال العملاء.",
      description_ckb: "ناوەڕۆکی ڕوون بۆ گەڕان و زیرەکی دەستکرد — یەکەم هەنگاوین کاتێک کڕیاران دەپرسن.",
      description_bad: "ناڤەرۆکەکا روون بۆ گەریانێ و زیرەکیا دەستکرد — ئەم ئێکەم پێنگاڤین دەمێ کریار پرسیار دکەن.",
      primary_btn_text_ar: "اطلب استشارة",
      primary_btn_text_ckb: "داوای ڕاوێژ بکە",
      primary_btn_text_bad: "داخوازا شیرەتکاریێ بکە",
      primary_btn_url: "#contact",
      secondary_btn_text_ar: "تصفح الأعمال",
      secondary_btn_text_ckb: "پۆرتفۆلیۆ ببینە",
      secondary_btn_text_bad: "کارێن مە ببینە",
      secondary_btn_url: "#portfolio",
      featured_image_url: "",
      featured_label_ar: "Laptop Duhok",
      featured_label_ckb: "Laptop Duhok",
      featured_label_bad: "Laptop Duhok",
      background_css: "",
    },
  ];

  var slidesData = [];
  var idx = 0;
  var timer = null;
  var prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  function esc(s) {
    var d = document.createElement("div");
    d.textContent = s == null ? "" : String(s);
    return d.innerHTML;
  }

  function getLang() {
    var l = document.documentElement.getAttribute("lang");
    if (l === "ckb") return "ckb";
    if (l === "bad") return "bad";
    return "ar";
  }

  function pick(row, base) {
    var lang = getLang();
    var ar = row[base + "_ar"];
    var ckb = row[base + "_ckb"];
    var bad = row[base + "_bad"];
    if (lang === "ckb" && ckb && String(ckb).trim()) return String(ckb);
    if (lang === "bad" && bad && String(bad).trim()) return String(bad);
    return String(ar || ckb || bad || "").trim();
  }

  function renderSlideMarkup(rows) {
    var html = "";
    rows.forEach(function (row, i) {
      var tag = i === 0 ? "h1" : "h2";
      var idAttr = i === 0 ? ' id="hero-title"' : "";
      html +=
        '<div class="hero-slide' +
        (i === 0 ? " is-active" : "") +
        '" data-slide="' +
        i +
        '">' +
        "<" +
        tag +
        idAttr +
        ' class="hero-title">' +
        esc(pick(row, "title")) +
        "</" +
        tag +
        ">" +
        '<p class="hero-desc">' +
        esc(pick(row, "description")) +
        "</p>" +
        "</div>";
    });
    return html;
  }

  function renderFeatured(row) {
    var slot = document.getElementById("hero-featured-card");
    if (!slot) return;
    var url = (row.featured_image_url || "").trim();
    var label = pick(row, "featured_label");
    if (url) {
      slot.innerHTML =
        '<div class="hero-featured-inner">' +
        '<img src="' +
        esc(url) +
        '" alt="" class="hero-featured-img" loading="lazy" width="400" height="240" />' +
        (label
          ? '<span class="hero-featured-caption">' + esc(label) + "</span>"
          : "") +
        "</div>";
    } else {
      slot.innerHTML =
        '<div class="hero-placeholder is-visible">' +
        '<svg viewBox="0 0 400 280" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">' +
        "<defs>" +
        '<linearGradient id="heroGradDyn" x1="0%" y1="0%" x2="100%" y2="100%">' +
        '<stop offset="0%" style="stop-color:#0e4aff"/>' +
        '<stop offset="100%" style="stop-color:#ff5a00"/>' +
        "</linearGradient>" +
        "</defs>" +
        '<rect width="400" height="280" rx="20" fill="rgba(255,255,255,0.12)"/>' +
        '<rect x="40" y="48" width="320" height="180" rx="12" fill="url(#heroGradDyn)" opacity="0.4"/>' +
        "</svg>" +
        (label
          ? '<p class="hero-featured-fallback-label">' + esc(label) + "</p>"
          : "") +
        "</div>";
    }
  }

  function applyBackground(row) {
    var grad = document.querySelector("#ld-hero-root .hero-gradient");
    if (!grad) return;
    var css = (row.background_css || "").trim();
    if (css) {
      grad.style.background = css;
      grad.classList.add("hero-gradient--custom");
    } else {
      grad.style.background = "";
      grad.classList.remove("hero-gradient--custom");
    }
  }

  function updateCtas(row) {
    var rowEl = document.getElementById("hero-cta-row");
    if (!rowEl) return;
    var p = rowEl.querySelector("[data-hero-primary]");
    var s = rowEl.querySelector("[data-hero-secondary]");
    if (p) {
      p.textContent = pick(row, "primary_btn_text");
      p.setAttribute("href", row.primary_btn_url || "#contact");
    }
    if (s) {
      s.textContent = pick(row, "secondary_btn_text");
      s.setAttribute("href", row.secondary_btn_url || "#portfolio");
    }
  }

  function updateKicker(row) {
    var k = document.getElementById("hero-kicker-el");
    if (k) k.textContent = pick(row, "kicker");
  }

  function show(i) {
    var slides = document.querySelectorAll("#hero-slides-host .hero-slide");
    var total = slides.length;
    if (!total) return;
    idx = (i + total) % total;
    slides.forEach(function (el, j) {
      el.classList.toggle("is-active", j === idx);
    });
    var dotsWrap = document.getElementById("hero-dots");
    if (dotsWrap) {
      dotsWrap.querySelectorAll(".hero-dot").forEach(function (d, j) {
        d.classList.toggle("is-active", j === idx);
      });
    }
    var row = slidesData[idx];
    if (row) {
      updateKicker(row);
      updateCtas(row);
      renderFeatured(row);
      applyBackground(row);
    }
  }

  function buildDots(total) {
    var dotsWrap = document.getElementById("hero-dots");
    if (!dotsWrap) return;
    dotsWrap.innerHTML = "";
    for (var i = 0; i < total; i++) {
      var b = document.createElement("button");
      b.type = "button";
      b.className = "hero-dot" + (i === 0 ? " is-active" : "");
      b.setAttribute("aria-label", "Slide " + (i + 1));
      b.addEventListener(
        "click",
        (function (n) {
          return function () {
            show(n);
          };
        })(i)
      );
      dotsWrap.appendChild(b);
    }
  }

  function startAutoplay() {
    if (timer) clearInterval(timer);
    if (prefersReducedMotion) return;
    var slides = document.querySelectorAll("#hero-slides-host .hero-slide");
    if (slides.length < 2) return;
    timer = setInterval(function () {
      show(idx + 1);
    }, 7000);
    window.addEventListener(
      "beforeunload",
      function () {
        if (timer) clearInterval(timer);
      },
      { once: true }
    );
  }

  function bindArrows() {
    var prev = document.querySelector(".hero-arrow-prev");
    var next = document.querySelector(".hero-arrow-next");
    if (prev) prev.addEventListener("click", function () { show(idx - 1); });
    if (next) next.addEventListener("click", function () { show(idx + 1); });
  }

  function mount(rows) {
    slidesData = rows;
    var host = document.getElementById("hero-slides-host");
    if (!host || !rows.length) return;
    host.innerHTML = renderSlideMarkup(rows);
    buildDots(rows.length);
    idx = 0;
    show(0);
    bindArrows();
    startAutoplay();
    window.LD_heroRefresh = function () {
      show(idx);
    };
  }

  function load() {
    var client = window.ldSupabase;
    var host = document.getElementById("hero-slides-host");
    if (!host) return;

    if (!client) {
      mount(FALLBACK);
      return;
    }

    client
      .from("hero_slides")
      .select(
        "kicker_ar, kicker_ckb, title_ar, title_ckb, description_ar, description_ckb, primary_btn_text_ar, primary_btn_text_ckb, primary_btn_url, secondary_btn_text_ar, secondary_btn_text_ckb, secondary_btn_url, featured_image_url, featured_label_ar, featured_label_ckb, background_css"
      )
      .eq("is_published", true)
      .order("sort_order", { ascending: true })
      .then(function (res) {
        if (res.error || !res.data || !res.data.length) {
          mount(FALLBACK);
          return;
        }
        mount(res.data);
      })
      .catch(function () {
        mount(FALLBACK);
      });
  }

  function onReady(fn) {
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", fn);
    } else {
      fn();
    }
  }

  onReady(function () {
    load();
    document.addEventListener("ld-lang-change", function () {
      if (slidesData.length && typeof window.LD_heroRefresh === "function") {
        window.LD_heroRefresh();
      }
    });
  });
})();
